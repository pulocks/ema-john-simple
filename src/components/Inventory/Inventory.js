import React from 'react';
//import fakeData from '../../fakeData';

const Inventory = () => {
    /*const handleAddInventory = () => {
        const product = fakeData;
        console.log('Total quantity', product.length);
        fetch('http://localhost:3001/addProduct', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify(product) 
        })
        .then(res => res.json())
        .then(data => {
            console.log('post successful', data);
        });
    }*/
    return (
        <div>
            <h1>Inventory is coming soon</h1>
            {/*<button className="main-button" onClick={handleAddInventory}>Add Inventory</button>*/}
        </div>
    );
};

export default Inventory;